<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ConnectNest</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white ">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 " href="/logout ">Logout</a>
        </div>
        
        <h3 class="text-3xl text-white mb-5 mt-5"><span 
        <h5 class="mb-5"> Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea  placeholder="Whats on your mind?"class="block p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input type="submit" class="px-3 py-2 bg-yellow-500 block rounded-md w-40 mt-5" value="Update post">

        </form>
        
        
    </div>
    
</body>
</html>